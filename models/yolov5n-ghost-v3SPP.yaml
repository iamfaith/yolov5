# YOLOv5 🚀 by Ultralytics, GPL-3.0 license

# Parameters
nc: 1  # number of classes
depth_multiple: 0.33  # model depth multiple
width_multiple: 0.25  # layer channel multiple
anchors:
  - [10,13, 16,30, 33,23]  # P3/8
  - [30,61, 62,45, 59,119]  # P4/16
  - [116,90, 156,198, 373,326]  # P5/32


backbone:
  [[-1, 1, Conv, [16, 3, 2]],       # 0-P1/2
   [-1, 1, GhostConv, [32, 3, 2]],  # 1-P2/4
   [-1, 2, C3Ghost, [32]],

   [-1, 1, GhostConv, [64, 3, 2]],  # 3-P3/8
   [-1, 3, C3Ghost, [64]],

   [-1, 1, GhostConv, [128, 3, 2]], # 5-P4/16
   [-1, 3, C3Ghost, [128]],

   [-1, 1, GhostConv, [192, 3, 2]], # 7-P5/32
   [-1, 2, C3Ghost, [192]],         # 更少层数
   [ -1, 1, SPP_3Kernel, [48, 48]],
  ]



head:
  [[-1, 1, GhostConv, [96, 1, 1]],
   [-1, 1, nn.Upsample, [None, 2, 'nearest']],
   [[-1, 6], 1, Concat, [1]],  
   [-1, 2, C3Ghost, [96, False]],  # P4

   [-1, 1, GhostConv, [48, 1, 1]],
   [-1, 1, nn.Upsample, [None, 2, 'nearest']],
   [[-1, 4], 1, Concat, [1]],  
   [-1, 2, C3Ghost, [48, False]],  # P3 (small)

   [-1, 1, GhostConv, [64, 3, 2]],
   [[-1, 13], 1, Concat, [1]],  
   [-1, 2, C3Ghost, [96, False]],  # P4 (medium)

   [-1, 1, GhostConv, [128, 3, 2]],
   [[-1, 8], 1, Concat, [1]],  
   [-1, 2, C3Ghost, [192, False]], # P5 (large)

   [[14, 17, 20], 1, Detect, [nc, anchors]],  # Detect(P3, P4, P5)
  ]



# head:
#   [[-1, 1, GhostConv, [128, 1, 1]],   # 7 reduce  64->32
#    [-1, 1, nn.Upsample, [None, 2, 'nearest']],  # 8
#    [[-1, 4], 1, Concat, [1]],         # 9 cat backbone P3   48
#    [-1, 1, C3Ghost, [128]],           # 10
#    [-1, 1, GhostConv, [64, 1, 1]],    # 11 reduce
#    [-1, 1, nn.Upsample, [None, 2, 'nearest']],  # 12
#    [[-1, 2], 1, Concat, [1]],         # 13 cat backbone P2
#    [-1, 1, C3Ghost, [64]],            # 14
#    [-1, 1, Conv, [64, 3, 2]],         # 15 downsample
#    [[-1, 11], 1, Concat, [1]],        # 16
#    [-1, 1, C3Ghost, [128]],           # 17
#    [-1, 1, Conv, [128, 3, 2]],        # 18 downsample
#    [[-1, 7], 1, Concat, [1]],         # 19
#    [-1, 1, C3Ghost, [256]],           # 20
#    [[14, 17, 20], 1, Detect, [nc, anchors]]  # [nc, [64, 128, 256]]]
#   ]

# YOLOv5 v6.0 head
######################################
# head:
#   [[-1, 1, Conv, [512, 1, 1]],
#    [-1, 1, nn.Upsample, [None, 2, 'nearest']],
#    [[-1, 6], 1, Concat, [1]],  # cat backbone P4
#    [-1, 3, C3, [512, False]],  # 13

#    [-1, 1, Conv, [256, 1, 1]],
#    [-1, 1, nn.Upsample, [None, 2, 'nearest']],
#    [[-1, 4], 1, Concat, [1]],  # cat backbone P3
#    [-1, 3, C3, [256, False]],  # 17 (P3/8-small)

#    [-1, 1, Conv, [256, 3, 2]],
#    [[-1, 14], 1, Concat, [1]],  # cat head P4
#    [-1, 3, C3, [512, False]],  # 20 (P4/16-medium)

#    [-1, 1, Conv, [512, 3, 2]],
#    [[-1, 10], 1, Concat, [1]],  # cat head P5
#    [-1, 3, C3, [1024, False]],  # 23 (P5/32-large)

#    [[17, 20, 23], 1, Detect, [nc, anchors]],  # Detect(P3, P4, P5)
#   ]

# YOLOv5 🚀 by Ultralytics, GPL-3.0 license

# Parameters
nc: 1  # number of classes
depth_multiple: 0.33  # model depth multiple
width_multiple: 0.25  # layer channel multiple
anchors:
  - [10,13, 16,30, 33,23]  # P3/8
  - [30,61, 62,45, 59,119]  # P4/16
  - [116,90, 156,198, 373,326]  # P5/32


backbone:
  [[-1, 1, Conv, [32, 3, 2]],        # 0-P1/2 (原16 → 32)
   [-1, 1, GhostConv, [64, 3, 2]],   # 1-P2/4 (原32 → 64)
   [-1, 3, C3Ghost, [64]],

   [-1, 1, GhostConv, [128, 3, 2]],  # 3-P3/8 (原64 → 128)
   [-1, 6, C3Ghost, [128]],

   [-1, 1, GhostConv, [256, 3, 2]],  # 5-P4/16 (原128 → 256)
   [-1, 6, C3Ghost, [256]],

   [-1, 1, GhostConv, [512, 3, 2]],  # 7-P5/32 (原192 → 512)
   [-1, 3, C3Ghost, [512]],

   [-1, 1, GhostConv, [1024, 3, 2]], # 新增一层，扩大到 1024 P6/64
   [-1, 3, C3Ghost, [1024]],         # 保持较少层数，但通道更大
  ]

# backbone:
#   [0]  [ -1, 1, Conv, [32, 3, 2]],         # P1/2
#   [1]  [ -1, 1, GhostConv, [64, 3, 2]],    # P2/4
#   [2]  [ -1, 3, C3Ghost, [64]],

#   [3]  [ -1, 1, GhostConv, [128, 3, 2]],   # P3/8
#   [4]  [ -1, 6, C3Ghost, [128]],

#   [5]  [ -1, 1, GhostConv, [256, 3, 2]],   # P4/16
#   [6]  [ -1, 6, C3Ghost, [256]],

#   [7]  [ -1, 1, GhostConv, [512, 3, 2]],   # P5/32
#   [8]  [ -1, 3, C3Ghost, [512]],

#   [9]  [ -1, 1, GhostConv, [1024, 3, 2]],  # P6/64 (新增)
#   [10] [ -1, 3, C3Ghost, [1024]],          # P6 refined

head:
  [[-1, 1, GhostConv, [512, 1, 1]],
   [-1, 1, nn.Upsample, [None, 2, 'nearest']],
   [[-1, 6], 1, Concat, [1]],   # 对应 backbone 的 P4
   [-1, 3, C3Ghost, [512, False]],

   [-1, 1, GhostConv, [256, 1, 1]],
   [-1, 1, nn.Upsample, [None, 2, 'nearest']],
   [[-1,4], 1, Concat, [1]],   # 对应 backbone 的 P3
   [-1, 3, C3Ghost, [256, False]],

   [-1, 1, GhostConv, [512, 3, 2]],
   [[-1, 13], 1, Concat, [1]],  # 对应 head 的 P4
   [-1, 3, C3Ghost, [512, False]],

   [-1, 1, GhostConv, [1024, 3, 2]],
   [[-1, 10], 1, Concat, [1]],  # 对应 backbone 的 P5
   [-1, 3, C3Ghost, [1024, False]],

   [[18, 21, 24], 1, Detect, [nc, anchors]],
  ]


# head:
#   [11] [ -1, 1, GhostConv, [512, 1, 1]],
#   [12] [ -1, 1, nn.Upsample, [None, 2, 'nearest']],
#   [13] [[-1, 6], 1, Concat, [1]],          # 对应 backbone P4
#   [14] [ -1, 3, C3Ghost, [512, False]],    # P4 head

#   [15] [ -1, 1, GhostConv, [256, 1, 1]],
#   [16] [ -1, 1, nn.Upsample, [None, 2, 'nearest']],
#   [17] [[-1, 4], 1, Concat, [1]],          # 对应 backbone P3
#   [18] [ -1, 3, C3Ghost, [256, False]],    # P3 head (small)

#   [19] [ -1, 1, GhostConv, [512, 3, 2]],
#   [20] [[-1, 13], 1, Concat, [1]],         # 对应 head P4
#   [21] [ -1, 3, C3Ghost, [512, False]],    # P4 head (medium)

#   [22] [ -1, 1, GhostConv, [1024, 3, 2]],
#   [23] [[-1, 10], 1, Concat, [1]],         # 对应 backbone P6
#   [24] [ -1, 3, C3Ghost, [1024, False]],   # P6 head (large)

#   [25] [[18, 21, 24], 1, Detect, [nc, anchors]],  # Detect(P3, P4, P6)